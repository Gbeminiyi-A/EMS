export default interface Project {
    projectId: string;
    projectName: string;
    projectDomain: string;
    clientName: string;
    projectManager: string;
    startDate: string;
    endDate: string;
}